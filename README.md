# tfmon
A Text File MONitor(tfmon) to watch a single or multiple small text files in the command line and follow changes.

> !!!Prototype!!!

## Todo:
- function for reading files
- Option to enable/disable file name, timestamp or counter
- Working for yet not existing files or removed files
- Better error management
- Possibility to give a name to the file to replace path
- static position for values
- Optimize drawing (use changed flag)
- Use /dev/tty instead of stdout
- Possibility to use a config file to rewrite all option/parameter (INI style)
